# mod_php itself is loaded by the php83-apache2 package, which drops its
# own LoadModule line into /etc/apache2/conf.d/ automatically. This file
# just wires PHP files to that handler and sets the default index.

<FilesMatch \.php$>
    SetHandler application/x-httpd-php
</FilesMatch>

DirectoryIndex index.php index.html